---
description: Say hello from the hello-world plugin
argument-hint: "[name]"
---

Say hello to $ARGUMENTS.

If no name was given, greet "world" instead. Keep the greeting to a single warm
sentence, then briefly mention that this came from the `hello` command of the
`hello-world` plugin (installed from the `leo-skills` marketplace).
